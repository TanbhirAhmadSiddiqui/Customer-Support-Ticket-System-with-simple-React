import React from "react";

const TaskStatus = ({ taskStatus }) => {
  console.log(status);
  return (
    <div>
      <h1>status:{taskStatus.title}</h1>
    </div>
  );
};

export default TaskStatus;
